<!-- The argument to simple_fields_for—:attachments— tells Rails the name of the association that should be used for the nested fields within the original form.  -->
<%= f.simple_fields_for :attachments, child_index: index do |ff| %>
  <%= ff.input :file, as: :file, label: "File ##{index += 1}" %>
  <%= ff.input :file_cache, as: :hidden %>
<% end %>
